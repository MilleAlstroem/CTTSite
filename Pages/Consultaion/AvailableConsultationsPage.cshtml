@page
@model CTTSite.Pages.Consultaion.AvailableConsultationsPageModel
@{
}



<br />
<h1 class="text-center"><b>Available Consultations</b></h1>
<br />
<div class="test">
    <table class="table table-bordered table-hover" width="60%">
        <thead>
            <tr class="btn-secondary active">
                <th>Date</th>
                <th>StartDateTime</th>
                <th>EndDateTime</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @if (@Model.ConsultationsList != null)
            {
                foreach (Models.Consultation consultation in Model.ConsultationsList)
                {
                    <tr class="btn-secondary">
                        <td>
                            @consultation.Date
                        </td>
                        <td>
                            @consultation.StartTime
                        </td>
                        <td>
                            @consultation.EndTime
                        </td>
                        <td>
                            <a class="btn" type="button" asp-page="BookConsultationPage" asp-route-id="@consultation.ID"><i class="fa fa-pencil-square-o"></i></a>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>